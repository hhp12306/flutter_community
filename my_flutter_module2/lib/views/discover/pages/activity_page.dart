import 'package:flutter/material.dart';

/// 活动页面
class ActivityPage extends StatelessWidget {
  const ActivityPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('活动页面', style: TextStyle(fontSize: 18.0)),
    );
  }
}

